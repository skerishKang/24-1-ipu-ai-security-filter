# AGENTS.md

## 프로젝트 목표

- 이 프로젝트의 이름은 `IPU AI Security Filter`다.
- 브랜드 `IPU`는 "I'll Protect You"를 의미한다.
- 목표는 기업이 민감정보를 내부에서 보호, 치환, 복원한 뒤 외부 SOTA AI를 더 안전하게 활용할 수 있게 하는 웹 기반 보안필터 MVP를 만드는 것이다.
- 초기 제품은 범용 AI 포털이 아니라 "보안 치환 워크벤치"에 가깝게 정의한다.

## 구현 우선순위

1. 문서와 구조를 먼저 고정한다.
2. 수동 모드 워크벤치 UX를 설계한다.
3. 보안 엔진의 탐지, 치환, 세션 매핑 인터페이스를 정의한다.
4. 백엔드와 프론트엔드의 API 계약을 정리한다.
5. 자동 모드용 외부 모델 커넥터 확장 포인트를 설계한다.
6. 실제 자동 호출과 운영 기능은 후순위로 둔다.

## 참고 프로젝트 원칙

- `/mnt/g/Ddrive/BatangD/task/workdiary/24-secure-bridge`는 참고용이다.
- 기존 구조, 네이밍, 파일 배치를 그대로 복붙하지 않는다.
- 재사용 가능한 것은 개념, 처리 흐름, 모듈 경계 수준에서만 식별한다.
- 새 프로젝트는 웹 기반 중심 구조로 독립 설계한다.

## 문서 우선 원칙

- 코드 작성 전에 `README.md`, `AGENTS.md`, `docs/` 문서를 먼저 작성하고 업데이트한다.
- 아키텍처 변경, MVP 범위 변경, 모듈 책임 변경이 생기면 코드보다 문서를 먼저 수정한다.
- 큰 기능을 추가할 때는 먼저 목적, 인터페이스, 범위를 문서로 확정한다.

## 모드 우선순위

- 초기 구현은 반드시 수동 모드를 먼저 완성한다.
- 수동 모드는 치환 결과 확인, 탐지 리포트 확인, 외부 모델용 복사 프롬프트 제공을 포함한다.
- 자동 모드는 후순위이며, 현재 단계에서는 API 경계와 확장 포인트까지만 설계한다.
- 자동 모드가 수동 모드의 검증 가능성과 투명성을 해치지 않도록 유지한다.

## 구조 원칙

- `frontend`: 웹 워크벤치와 운영 UI
- `backend`: 웹 API, 세션, 업로드, 오케스트레이션
- `engine`: 민감정보 탐지, 치환, 역치환, 정책 핵심 로직
- `docs`: 제품, 아키텍처, 범위, 백로그 문서
- `prompts`: 보안 점검, 치환 가이드, 외부 모델 전달 템플릿

## 구현 가이드

- 지금 단계에서는 대규모 구현보다 구조 정리와 설계 일관성을 우선한다.
- 자동 모드 연동은 추후 외부 모델 커넥터 계층에서 처리한다.
- 파일 업로드, 음성 입력, 문서 파싱은 MVP 범위에서 인터페이스 우선으로 설계한다.
- 민감정보 치환은 세션 단위 매핑과 감사 가능한 리포트 생성을 기본 전제로 둔다.
